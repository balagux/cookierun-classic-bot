# CookieRun Classic Bot v1.4.11

โปรแกรมช่วยเล่นอัตโนมัติสำหรับ CookieRun Classic บน Windows ผ่าน ADB + OpenCV

> ⚠️ โปรเจกต์ทดลองด้าน Automation/Computer Vision การใช้งานกับบัญชีจริงอาจขัดข้อกำหนดเกม
> ผู้ใช้ต้องรับผิดชอบความเสี่ยงด้วยตนเอง

---

## 🛠️ ติดตั้ง

ดาวน์โหลด `CookieRunClassicBot-Setup.exe` ด้านล่าง แล้วติดตั้งได้เลย (ไม่ต้องลง Python)

- รองรับ Windows 10/11 64-bit
- ต้องใช้ LDPlayer/Android Emulator ความละเอียด **1280×720 เท่านั้น**
- ตัวติดตั้งรองรับภาษาไทย/อังกฤษ ติดตั้งแบบรายผู้ใช้ ไม่ต้องสิทธิ์ Administrator

### SHA-256

```
D989B932FF9257A0A836B5F406CDBFB24DED06F497D144AC9C847A733056701F
```

---

## ✨ สิ่งใหม่ใน v1.4.11

### 1. ปิดป๊อปอัป **News** (ประกาศข่าว/อัปเดต) ได้เอง

- เพิ่มการรู้จำหน้าต่าง **News** (เช่น แบนเนอร์ Mango Sticky Rice) โดยเฉพาะ
  ใช้ปุ่ม **X วงกลมมุมขวาบน `(1125, 55)`** ซึ่งอยู่คนละตำแหน่งกับป้ายประกาศทั่วไป
- จากเดิมที่บอทวนกด BACK ไม่รู้วิธีปิด ตอนนี้จำหน้าได้และกด X ปิดถูกจุดเอง

### 2. แก้บอทค้างเมื่อเจอป๊อปอัปอีเวนท์/ประกาศที่ปิดไม่ได้

- เปลี่ยนจากการกด X จุดเดียว (1127,66) เป็น **ไล่กด X หลายตำแหน่ง** ตามขนาดหน้าต่างแต่ละแบบ
- ถ้ายังกด X ไม่ปิด → กด **BACK** สำรอง → ถ้ายังไม่หาย → **รีสตาร์ทเกม** ให้กลับมาทำงานต่อเอง
- เพิ่มการเฝ้าจอ Main Menu: ถ้ากด START แล้วยังค้างที่ Main Menu (มีป๊อปอัปบังปุ่มเริ่ม)
  บอทจะปิดป๊อปอัปก่อนแล้วค่อยเริ่มรอบ ไม่กดปุ่มมั่วซ้ำ
- ทุกครั้งที่เจอหน้าจอไม่รู้จัก/ปิดป๊อปอัปไม่ได้ บอทจะ**บันทึกภาพลงโฟลเดอร์ `debug_screens`**
  ให้อัตโนมัติ เพื่อใช้ปรับแต่งในเวอร์ชันถัดไป

### 3. เพิ่มปุ่ม **รับหัวใจจากกล่องจดหมาย** 📮

- ปุ่มสีน้ำเงินแยกในแถบ RUN CONTROL (ถัดจาก "ส่งหัวใจ")
- เปิดหน้า **Friends** → กดปุ่ม → บอทเปิดกล่องจดหมาย → เปิดแท็บ Lives → กดรับทั้งหมด
  แล้ว **ไล่กด Confirm สีเขียวซ้ำ** จนครบทุกรายการ → ปิดกล่องกลับมาหน้า Friends
- ถ้าในกล่องไม่มีหัวใจ บอทจะปิดกล่องทันที ไม่กดปุ่มอื่นเพิ่ม
- เป็นกระบวนการแยก ไม่เริ่มรอบ ไม่ซื้อไอเทม ไม่กระทบสถิติ Coins/EXP และกด STOP ได้ตลอด
- มีขีดจำกัดการวนซ้ำ + ตรวจหน้าจอทุกครั้ง หากเปิดกล่องไม่ได้/Confirm ไม่หาย จะหยุดอย่างปลอดภัย
  และบันทึกภาพลง `debug_screens`

---

## 📖 เปลี่ยนจากเวอร์ชันก่อน

- v1.4.10: แก้เริ่มรอบจากหน้า Main Menu ปกติ
- v1.4.9: กันการตรวจจับ Relay แบบผิดจังหวะก่อนเกมเริ่ม
- v1.4.8: แก้ปุ่ม Start หลุดออกจากเกมด้วยการเช็คความสว่างแบนเนอร์

---

## 🕹️ ฟีเจอร์ที่มีอยู่แล้ว

- เชื่อมต่อ ADB (ไม่เปิดหน้าต่าง CMD), ซื้อ Fast Start / Cookie Relay / Random Boost
- เฝ้าจอระหว่างรอบ, รับ Mystery Box / Level Up / Daily / Relic
- ตรวจ Anti-Bot / Connection Lost / Inactive และกู้สถานะ
- OCR อ่าน Coins/EXP (ในเครื่อง ไม่ส่งข้อมูลออก), สรุปสถิติรอบ & เวลา
- แท็บสถิติกล่อง แยก ไม้/เงิน/ทอง/รุ้ง
- ส่งหัวใจให้เพื่อนจากหน้า Friends

---

> โปรเจกต์นี้ไม่มีการรีด/เล่น W/S อัตโนมัติ ผู้ใช้ควบคุมการเล่นเอง
> และบอทดูแลเฉพาะเมนู ไอเทม ผลรอบ และรางวัล
