## CookieRun Classic Bot v1.4.8

- แก้บั๊กกด Start แล้วบอทกดปุ่ม "ออกจากเกม" ทันที (แก้ให้จริงจัง)
- สาเหตุเดิม: Stage `GAME_START` กับ `GAME_RELAY` ใช้ region ตรวจจับเดียวกัน และ template ของ Relay ไป match กับหน้าจอเริ่มเกม บอทจึงเข้าใจผิดว่าเป็นโหมด Relay แล้วกด Quit
- v1.4.7 เพิ่ม safety guard ด้วย template matching แต่ยังไม่หาย เพราะ template ของ `GAME_START` เองก็ match หน้าจอจริงไม่ตรง
- **v1.4.8 เปลี่ยนวิธี**: ใช้ **ความสว่างของแบนเนอร์** แยกหน้าจอเริ่มเกมกับหน้าจอ Relay
  - หน้าจอเริ่มเกม (GAME_START): แบนเนอร์ **สว่าง** (center ~184)
  - หน้าจอ Relay (กำลังวิ่งจริง): แบนเนอร์ **มืด** (center ~110)
  - ถ้าแบนเนอร์ยังสว่าง = เกมยังไม่เริ่มวิ่ง → **ยกเลิกการออกจากเกม** อย่างปลอดภัย
- ป้องกันการออกจากเกมโดยไม่ตั้งใจเมื่อเปิดใช้ Cookie Relay + Relay Quick Exit

ความละเอียดภายใน LDPlayer ที่รองรับ: **1280×720**

SHA-256 (`CookieRunClassicBot.exe`):
`F3A98847B082070B74AE9460A04C3F72DF8DF9B62F44B48F74736BE7BB315FE2`
